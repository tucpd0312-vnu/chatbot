apiVersion: v1
kind: Service
metadata:
  name: core-service
  namespace: airc-chatbot
  labels:
    app: core-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: core-service
